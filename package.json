{
  "name": "ai-a-to-z-affiliate-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend:install": "python3 -m pip install -r backend/requirements.txt",
    "backend:dev": "cd backend && uvicorn app.main:app --reload",
    "backend:test": "PYTHONPATH=backend pytest -q backend/tests"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.2",
    "puppeteer": "^25.10.0",
    "vite": "^7.1.3"
  }
}
